from typing import Literal


def predict_price(period: int = 3, period_type: Literal["day", "month", "year"] = "month") -> dict:
    """Predict price based on

    Args:
        period (int, optional): _description_. Defaults to 3.
        period_type (Literal[&#39;day&#39;, &#39;month&#39;, &#39;year&#39;], optional): _description_. Defaults to 'month'.

    Returns:
        list: _description_
    """
    return {}
